# 네트워크

## 🔍 문제에 접근한 방식과 이유
- 하나씩 분리 후 각 분리된 노드에 dfs하여 개수 찾기
- 각 노드에 연결된 노드를 저장하고 이를 통해 연결된 노드들을 bfs로하여 각 노드별 연결된 수 구하기기

## ❌ 오답인 경우 (해당하는 경우에만 작성)
- 시작 노드 방문 처리 안함
- visited의 오류로 방문한 곳에 다시 방문함
- 마지막에 동일 네트워크 수 구할때 네트워크가 변하면 체크하도록 하였더니 나중에 연결된 노드 (1,2,1)가 체크 안됨됨

## 추가 +@
- 전체 노드 개수가 주어지므로 그냥 한쪽만 구해서 bfs해도 전체 - 한쪽 노드 수로 양쪽 연결 수 를 알 수 있다

### 정답 코드
```python
def solution(n, computers):
    answer = 0
    nodes = [[] for _ in range(n)]
    # print(nodes)
    
    # 주어진 정보 기반으로 양방향 그래프 생성 혹은 각 리스트에 노드 번호 저장
    for idx in range(n):
        for con_idx in range(n):
            if computers[idx][con_idx]==1: # and con_idx != idx:
                nodes[idx].append(con_idx)
    # print(nodes)
    
    visited = [0 for _ in range(n)]
    nw = [0 for _ in range(n)]
    network=0
    # 각 노드별 연결된 노드 번호 저장 후 bfs로 순회 -> visited 사용, 전체 노드 순회
    # print(visited,'before')
    ntw=0
    for nds_idx in range(n):
        ntw+=1
        # 각 인덱스 별로 bfs
        qu=[nds_idx]
            # nw=True
        while qu:
            st=qu.pop()
            # if visited[st]==True:
            #     break
            for nxt in nodes[st]:
                # print(nxt,visited[nxt],'nxt')
                if visited[nxt]==0:
                    visited[nxt]=ntw
                    qu.append(nxt)
                else:
                    continue
            # visited[st]=ntw
            # print(visited,'visted')
        # ntw+=1
    ans = 0
    comp=-1
    visited=list(set(visited))
    
    return len(visited)
```

### 오답 코드
```python
# 자기 자신과 연결된 노드는 1로 표시
# 전체 노드 수 n
# 주어진 정보 기반으로 양방향 그래프 생성 혹은 각 리스트에 노드 번호 저장
# 각 노드별 연결된 노드 번호 저장 후 bfs로 순회 -> visited 사용, 전체 노드 순회

def solution(n, computers):
    answer = 0
    nodes = [[] for _ in range(n)]
    # print(nodes)
    
    # 주어진 정보 기반으로 양방향 그래프 생성 혹은 각 리스트에 노드 번호 저장
    for idx in range(n):
        for con_idx in range(n):
            if computers[idx][con_idx]==1: # and con_idx != idx:
                nodes[idx].append(con_idx)
    # print(nodes)
    
    visited = [0 for _ in range(n)]
    nw = [0 for _ in range(n)]
    network=0
    # 각 노드별 연결된 노드 번호 저장 후 bfs로 순회 -> visited 사용, 전체 노드 순회
    # print(visited,'before')
    ntw=0
    for nds_idx in range(n):
        ntw+=1
        # 각 인덱스 별로 bfs
        qu=[nds_idx]
            # nw=True
        while qu:
            st=qu.pop()
            # if visited[st]==True:
            #     break
            for nxt in nodes[st]:
                # print(nxt,visited[nxt],'nxt')
                if visited[nxt]==0:
                    visited[nxt]=ntw
                    qu.append(nxt)
                else:
                    continue
            # visited[st]=ntw
            # print(visited,'visted')
        # ntw+=1
    ans = 0
    comp=-1
    for to_comp in visited:
        if comp != to_comp:
            ans +=1
            comp=to_comp
    return ans
```