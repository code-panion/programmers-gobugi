# 접근 방식

바로 다음에 연속되지만 않으면 통과되기 때문에 리스트 제일 뒤만 확인하면 된다고 생각함. 

</br>
</br>

# 학습내용

스택을 그대로 구현하려고 생각함. 생각없이 pop했다가 마음을 고쳐먹고 for 문 돌렸더니 해결됨.

</br>
</br>

# 풀이 코드


```
def solution(arr):
    answer = []
    for i in range(len(arr)):
        if not answer:
            answer.append(arr[i])
        elif answer[-1] != arr[i]:
            answer.append(arr[i])
    return answer
```

