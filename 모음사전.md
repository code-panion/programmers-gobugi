# 모음사전

## 🔍 문제에 접근한 방식과 이유
- 모음만으로 만든 단어 중 입력된 단어의 위치이므로 전체 모음 단어들을 만들고 해당 단어를 .find로 찾아보고자 하였으나 안되서 그냥 for문으로 찾음
- 모음 단어 생성은 재귀로 진행 (5^5이므로 3125번 재귀이므로 문제 없음)

## 새로운 사항
find 메서드의 경우 문자열에서 특정 문자의 위치(인덱스)를 반환하지 리스트는 find 메서드가 없다...
대신 .index로 할 수 있다! (동일한 값 찾아 인덱스 반환)

출처
https://dreamjy.tistory.com/93  # list의 index 메서드
https://www.w3schools.com/python/ref_string_find.asp # 문자열에서 find 메서드

### 정답 코드
```python

# 길이 5까지 모든 모음으로 만들 수 있는 수를 만들어 리스트에 넣는다
# 리스트에서 몇번째인지 찾기
lst=[]

def solution(word):
    global lst
    answer = 0
    words=[]
    find_all(words)
    
    idx=0
    ans = lst.index(word)
    print(ans,'ans')
    for i in lst:
        if i == word:
            break
        idx+=1
    # print(idx)
    # ans = lst.find(word)
    # print(ans)
    return idx

def find_all(wrd):
    global lst
    vowel=['A','E','I','O','U']
    lst.append(''.join(chr for chr in wrd))
    if len(wrd)==5:
        return
    for i in range(5):
        wrd.append(vowel[i])
        find_all(wrd)
        wrd.pop()
        
```
