# 게임 맵 최단거리

## 🔍 문제에 접근한 방식과 이유
- 이동 가능한 곳 확인 후 이동한 다음 global 변수로 최솟값 설정 후 갱신하고자 함.
- 최종적으론 bfs를 통해 가장 처음 도착한 길이가 가장최소 길이이므로 bfs + 길이로 풀이이


## ❌ 오답 원인

- dfs 풀이, 시초 및 런타임 에러 (재귀 수 초과로 예상)
- bfs로 다시 접근
- 네 방향 이동에서 위로 먼저 가게했더니 자꾸 위로가서 오른쪽부터 반시계로 이동
- bfs 구현 중 .pop()에서 앞이 아닌 뒤에서 뺌뺌

## 참고 자료
https://velog.io/@keum0821/%EA%B7%B8%EB%9E%98%ED%94%84Graph-BFS%EC%9D%98-%EA%B0%9C%EB%85%90%EA%B3%BC-%EA%B5%AC%ED%98%84
큐의 뒤에 넣고 앞에서 빼기

### 정답 코드 (BFS)
---python
# bfs 풀이, 거리 추가하여 진행하는 방식 참고
def solution(maps):
    mx=10002
    qu = [[0,0,1]]
    # 방문지점 확인
    visited=[[False for _ in range(len(maps[0]))] for _ in range(len(maps)) ]
    # 방향 설정, 오른쪽부터 반시계
    dire = [[0,1],[1,0],[0,-1],[-1,0]]
    # 시작점 방문처리
    visited[0][0]=True
    # y,x로 풀이
    while qu:
        # pop(0)가 맨앞에서 꺼내니 큐 방식, 그냥 pop()하면 스택(마지막꺼 꺼냄)
        nw=qu.pop(0)
        # print(qu,'qu')
        # print(nw[0],nw[1],nw,'new coor')
        
        # 도착지점은 지도 길이 -1 (인덱스 0부터 시작)
        dst_y=len(maps)-1
        dst_x=len(maps[0])-1
        # 만약 도착했으면
        if nw[0]==dst_y and nw[1]==dst_x:
            # 최소거리 비교 => 근데 이거 필요없음, 애초에 처음 도착하면 visit 처리 + 최소거리
            if nw[2]<mx:
                mx=nw[2]
            # print(nw[2],'new distances!')
        
        for dr in dire:
            nxt_y = nw[0]+dr[0]
            nxt_x = nw[1]+dr[1]
            # 지도 길이보다 짧아야 범위 안(인덱스 시작은 0)
            if 0<=nxt_y<len(maps) and 0<=nxt_x<len(maps[0]):
                # print(nxt_y,nxt_x,'phase 1')
                # 방문 여부 및 이동 가능 확인
                if visited[nxt_y][nxt_x]==False and maps[nxt_y][nxt_x]==1:
                    # print(nxt_y,nxt_x,'phase2')
                    # print(nw[2],'tst')
                    # print([nxt_y,nxt_x,nw[2]+1],'wtf')
                    
                    # 바로 방문 처리 -> 격자맘ㄹ고 그래프에서 중복 발생가능하다고 함 아직 이해못함
                    # 현재 위치말고 이동할 다음 위치!
                    visited[nxt_y][nxt_x]=True
                    
                    qu.append([nxt_y,nxt_x,nw[2]+1])
                    # visited[nw[0]][nw[1]]=True
        # for i in visited:
            # print(i)
    # 도달 못하면 안 바뀌니 유지되면 도달 못한거
    if mx==10002:
        mx=-1
    return mx
```


### 오답 코드 (DFS)
# dfs 풀이, 시초 및 런타임 에러 (재귀 수 초과로 예상)
```python
# mnm=100002
# # bfs 2차원 이동, 최단거리 이동
# def solution(maps):
#     global mnm
#     # print(maps)
#     # print(len(maps[0]),len(maps))
#     answer = 0
#     visited=[[False for _ in range(len(maps[0]))] for _ in range(len(maps)) ]
#     current=[0,0]
#     distance=1
#     ans=find_mnm(maps,current,distance,visited)
#     # print(mnm,'mnm')
#     if mnm == 100002:
#         mnm=-1
#     return mnm

# def find_mnm(maps,cur,dst,visit):
#     global mnm
#     if cur[0]==len(maps)-1 and cur[1]==len(maps[0])-1: # y, x
        
#         if dst<=mnm:
#             mnm=dst
#         return
    
#     checker=[[1,0],[0,1],[-1,0],[0,-1]]
#     for x in checker:
#         # 범위 체크
#         if 0<=cur[0] + x[0] < len(maps) and 0 <= cur[1] + x[1] < len(maps[0]):
#             # print(cur[0] + x[0],cur[1] + x[1], 'possible')
#         # 방문 및 이동 가능성 확인 후 이동
#             if maps[cur[0] + x[0]][cur[1] + x[1]]==1 and visit[cur[0] + x[0]][cur[1] + x[1]]==False:
#                 # print(cur[0] + x[0],cur[1] + x[1], 'go')
#                 visit[cur[0] + x[0]][cur[1] + x[1]]=True
#                 find_mnm(maps,[cur[0] + x[0],cur[1] + x[1]],dst+1,visit)
#                 visit[cur[0] + x[0]][cur[1] + x[1]]=False
#     return
```