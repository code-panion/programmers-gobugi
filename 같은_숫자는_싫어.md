# 같은 숫자는 싫어

## 🔍 문제에 접근한 방식과 이유
- prev 변수를 통해 앞 숫자와 다르면 answer 리스트에 추가
- set() 자료구조도 고려하였으나 예제 1에서 동일한 숫자도 나오는 것에 포기


### 정답 코드
```python

def solution(arr):
    answer = []
    prev = -1
    for i in arr:
        if i == prev:
            continue
        else:
            answer.append(i)
            prev=i
    return answer
```