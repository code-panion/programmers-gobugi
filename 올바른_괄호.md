# 올바른 괄호

## 🔍 문제에 접근한 방식과 이유
- 하나씩 문자를 받아 '('라면 개수 +1, ')'라면 -1하여  끝났을 때 0이 아니면 () 조합이 완전하지 않은 것이므로 false 


### 정답 코드
```python


def solution(s):
    answer = True
    q=0
    for i in s:
        if i=='(':
            q+=1
        else:
            q-=1
        if q<0:
            answer=False
            break
    if q!=0:
        answer=False

    return answer
```